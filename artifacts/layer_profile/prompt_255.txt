On-device AI runs neural networks directly on the phone's system-on-chip, using the CPU, GPU, and the Hexagon neural processing unit. On-device AI runs neural networks directly on the phone's system-on-chip, using the CPU, GPU, and the Hexagon neural processing unit. On-device AI runs neural networks directly on the phone's system-on-chip, using the CPU, GPU, and the Hexagon neural processing unit. On-device AI runs neural networks directly on the phone's system-on-chip, using the CPU, GPU, and the Hexagon neural processing unit. On-device AI runs neural networks directly on the phone's system-on-chip, using the CPU, GPU, and the Hexagon neural processing unit. On-device AI runs neural networks directly on the phone's system-on-chip, using the CPU, GPU, and the Hexagon neural processing unit. On-device AI runs neural networks directly on the phone's system-on-chip, using the CPU, GPU, and the Hexagon neural processing unit. On-device AI runs neural networks directly on the phone's system-on-chip, using the CPU, GPU, and the Hexagon neural processing unit. On-device AI runs neural networks directly on the phone's system-on-chip, using the CPU, GPU, and the Hexagon neural processing unit. On-device AI runs neural networks directly on the phone's system-on-chip, using the CPU, GPU, and the Hexagon neural processing unit. On-device AI runs neural networks directly on the phone's system-on-chip, using the CPU, GPU, and the Hexagon neural processing unit. On-device AI runs neural networks directly on the phone's system-on-chip, using the CPU, GPU, and the Hexagon neural processing unit.